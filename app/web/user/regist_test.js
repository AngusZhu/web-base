'use strict';

describe('app.main module', function() {

  beforeEach(module('app.main'));

  describe('main controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('app.main.MainCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});